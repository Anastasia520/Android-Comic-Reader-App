<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hse.app.reader.comic.comicreader">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:launchMode="standard"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.hse.app.reader.comic.comicreader.MainActivity" />
        <activity
            android:name="com.hse.app.reader.comic.comicreader.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.hse.app.reader.comic.comicreader.ViewDetails" />
        <activity android:name="com.hse.app.reader.comic.comicreader.CategoryFilter"></activity>
        <activity android:name="com.hse.app.reader.comic.comicreader.ChapterActivity"></activity>
    </application>

</manifest>
